import { Routes } from '@angular/router';
import { Remote2ListComponent } from './remote2-list/remote2-list.component';

export const routes: Routes = [
    {
        path: 'rxjs',
        component: Remote2ListComponent,
      },
];
